"""{{package_name}} CLI."""
from typing import Optional

import click

from {{package_name}} import __version__, logs
from {{package_name}}.{{package_name}} import a_function


@click.command()
@click.version_option(__version__)
@click.option(
    "-v",
    "--verbosity",
    type=click.Choice(["error", "warning", "info", "debug"]),
    help="Log verbosity level [default: .env:VERBOSITY or info]",
)
def cli(verbosity: Optional[str]):
    """Engage with {{package_name}} using this cli."""
    logs.set_level(verbosity)
    print(a_function())
