# User guide

Let your users know how to use your package! For example, we can reference our example function with [`{{ package_name }}.{{ package_name }}.a_function`][]. You can use it like this:

```python
assert a_function() is None  # passes
```
